<?xml version="1.0" encoding="utf-8"?>
<!-- SonderAccessibilityService — see strings.xml for user-visible disclosure.
     Minimum scope: only window-state and window-content events are observed.
     No packageNames filter is declared so user-configured custom apps can be
     detected once their snapshots are synced; the service itself filters to
     the actually-configured targets before acting. Non-configured packages
     are ignored entirely (no overlay, no data collection). -->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagIncludeNotImportantViews|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/sonder_accessibility_service_description"
    android:notificationTimeout="200" />
