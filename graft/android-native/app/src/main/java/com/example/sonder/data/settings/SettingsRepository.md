# android-native/app/src/main/java/com/example/sonder/data/settings/SettingsRepository.kt

- dataStore · variable · L12-L12 — private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "sonder_settings")
- SettingsRepository · class · L15-L24 — class SettingsRepository(private val context: Context)
- setOnboardingDone · method · L21-L23 — suspend fun setOnboardingDone()
